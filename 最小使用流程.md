# ASM0003 双臂机器人 - 最小使用流程

## 环境要求

- Ubuntu 22.04
- ROS 2 Humble（已安装 `python3-catkin-pkg`）
- `colcon` 构建工具

## 快速开始

### MuJoCo 仿真（当前推荐）

```bash
MUJOCO_VIEWER=0 ./start_mujoco_sim.sh --docker
```

启动完成后打开 `http://127.0.0.1:8765`，在“视觉与点云”面板查看
头顶 D455 和可选的全局第三人称相机画面。默认发布：

```text
/mujoco_camera/color/image_raw
/mujoco_camera/depth/image_raw
/mujoco_camera/color/camera_info
/overview_camera/color/image_raw
```

头顶 D455 默认配置为 `640x480 @ 5 Hz`，全局相机默认开启（2 Hz）。如只需 RGB-D
并希望降低渲染开销，可设置 `MUJOCO_OVERVIEW_CAMERA_ENABLED=0`。

```bash
MUJOCO_CAMERA_WIDTH=1280 \
MUJOCO_CAMERA_HEIGHT=720 \
MUJOCO_CAMERA_FPS=5 \
MUJOCO_VIEWER=0 \
./start_mujoco_sim.sh --docker
```

如需关闭头顶 D455，设置 `MUJOCO_CAMERA_ENABLED=0`；如需关闭全局第三人称相机，设置
`MUJOCO_OVERVIEW_CAMERA_ENABLED=0`。

验证 RGB-D 同步、编码、数据长度和内参：

```bash
source /opt/ros/humble/setup.bash
export ROS_DOMAIN_ID=21
python3 tools/verify_mujoco_rgbd.py
```

在 RViz2 中添加两个 `Image` 显示项，分别选择：

```text
/mujoco_camera/color/image_raw
/mujoco_camera/depth/image_raw
```

深度图为 `32FC1` 米制浮点图；无效像素为 `NaN`。

网页“目标位姿”区域提供“抓取仿真红色物体”按钮。按钮从以下接口读取已知目标中心，
填入 XYZ 后复用现有自动夹取流程：

```text
GET /api/mujoco/grasp_target
world: x=0.000, y=-0.720, z=0.750 m
```

接口同时返回根据夹爪碰撞几何校正后的 TCP 目标。可通过
`MUJOCO_GRASP_TARGET_X/Y/Z` 覆盖物体中心，通过
`MUJOCO_GRASP_TCP_OFFSET_X/Y/Z` 覆盖 TCP 校正量。

```bash
# 1. 克隆仓库
git clone <repo-url> Local
cd Local

# 2. 编译
./v0.1control/scripts/build.sh

# 3. 加载环境
source install/setup.bash

# 4. 启动可视化（仅显示模型 + 关节状态）
./v0.1control/scripts/launch_display.sh

# 或启动 MoveIt（运动规划 + 拖拽控制）
./v0.1control/scripts/launch_moveit_rviz.sh
```

## 脚本说明

| 脚本 | 用途 |
|------|------|
| `v0.1control/scripts/build.sh` | 编译 `asm0003` 包（自动检测 ROS distro） |
| `v0.1control/scripts/launch_display.sh` | 启动 robot_state_publisher + RViz，订阅远端关节状态显示模型 |
| `v0.1control/scripts/launch_moveit_rviz.sh` | 启动 MoveIt + RViz，支持拖拽规划和执行 |
| `v0.1control/scripts/joint_state_merger.py` | 合并多主控的 joint_states 为统一话题 |

## 多机通信

本机需与机械臂主控在同一网络。脚本默认设置了：

```bash
export ROS_DOMAIN_ID=25
export ROS_STATIC_PEERS=192.168.10.200
```

如主控 IP 不同，通过环境变量覆盖：

```bash
ROS_STATIC_PEERS=192.168.1.100 ./v0.1control/scripts/launch_display.sh
```

## 远端（机械臂主控）使用方法

本仓库为控制端，机械臂主控端需运行自己的 ROS 2 节点。使用前确保主控端已正常工作。

### 通信确认

在主控端运行检查脚本：

```bash
export ROS_DOMAIN_ID=25
ros2 topic list | grep master
# 应能看到:
# /master0/joint_states  (左臂)
# /master2/joint_states  (右臂)
```

在控制端运行：

```bash
./v0.1control/scripts/check_v01_remote.sh
```

如果能收到 joint_states 数据，说明通信正常。

### 话题约定

| 话题 | 方向 | 说明 |
|------|------|------|
| `/master0/joint_states` | 主控 → 控制端 | 左臂关节状态 |
| `/master2/joint_states` | 主控 → 控制端 | 右臂关节状态 |
| `/arm_controller/follow_joint_trajectory` | 控制端 → 主控 | MoveIt 轨迹执行（action） |

### 网络要求

- 控制端与主控端必须在同一局域网
- ROS_DOMAIN_ID 必须一致（默认 25）
- 如跨网段或 DDS 发现异常，设置 `ROS_STATIC_PEERS=<主控IP>`

## 常见问题

**编译报错 `ModuleNotFoundError: No module named 'catkin_pkg'`**

```bash
sudo apt install python3-catkin-pkg
```

**启动报错 `package 'asm0003' not found`**

确保已 `source install/setup.bash`，或直接用脚本启动（脚本已自动 source）。
